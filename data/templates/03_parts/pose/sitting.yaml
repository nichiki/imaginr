# Pose Schema: Sitting

pose:
  type: sitting
  seat: ${seat|chair}
  body:
    posture: ${posture|upright}
    lean: ${lean|slight back}
  direction: ${body_direction|facing camera}
  head:
    tilt: ${head_tilt|}
    turn: ${head_turn|}
  legs: ${leg_position|crossed}
  arms: ${arm_position|on armrest}
  hands: ${hand_position|}
